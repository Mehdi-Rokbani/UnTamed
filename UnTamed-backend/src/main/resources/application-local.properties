# Local development overrides for working while Atlas connectivity is temporarily unavailable.
# Production/default behavior remains strict in application.properties.
app.mongo.ensure-indexes.enabled=false
app.mongo.ensure-indexes.fail-fast=false
app.jobs.booking-expiration.enabled=false
app.tags.seed.fail-fast=false
